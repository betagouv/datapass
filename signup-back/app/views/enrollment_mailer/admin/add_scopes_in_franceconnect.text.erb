Bonjour,

Une demande d’habilitation <%= @target_api_label %> pour le FS <%= @nom_raison_sociale %> (identifiant DataPass de la demande FranceConnect associée : <%= @previous_enrollment_id %>) vient d’être validée.

En conséquence, merci d’autoriser les scopes suivant pour le sus-mentionné FS :

<% @scopes.each do |scope| %>
- <%= scope %>
<% end %>

Merci de nous prévenir quand la tâche est terminée de votre côté afin que nous puissions clôturer le ticket de notre côté.

Pour consulter cette demande, veuillez cliquer sur le lien suivant <%= @url %> .

L’équipe DataPass
