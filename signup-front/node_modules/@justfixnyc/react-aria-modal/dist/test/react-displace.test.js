import React from "react";
import ReactDOM from "react-dom";
import { displace } from "../src/react-displace";
const Thingy = () => React.createElement("p", { id: "thingy" }, "I am thingy");
const DisplacedThingy = displace(Thingy);
test("Displace renders into displaced element", () => {
    const container = document.createElement("div");
    document.body.appendChild(container);
    try {
        ReactDOM.render(React.createElement(DisplacedThingy, null), container);
        expect(container.innerHTML).toBe("");
        const thingy = document.getElementById("thingy");
        if (!thingy)
            throw new Error("displaced element not found!");
        expect(thingy.innerHTML).toBe("I am thingy");
    }
    finally {
        ReactDOM.unmountComponentAtNode(container);
        document.body.removeChild(container);
    }
});
//# sourceMappingURL=react-displace.test.js.map