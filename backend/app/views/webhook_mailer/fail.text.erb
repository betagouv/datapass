Bonjour,

DataPass cherche à notifier votre <%= @api_manager_label %> depuis 10 minutes mais n’y parvient plus. Une habilitation DataPass n’a donc pas été transmise automatiquement à vos services.

Les informations techniques sont les suivantes:

**Endpoint**

<%= @webhook_url %>

**Payload**

La payload envoyée:

```
<%= @payload %>
```

**Status**

<%= @webhook_response_status %>

**Body**

<%= @webhook_response_body %>

Merci de transmettre ce message à votre équipe technique pour correction. Nous sommes disponibles pour vos questions techniques à l’adresse mail suivante : equipe-datapass@api.gouv.fr.

Cordialement,

L’équipe DataPass
